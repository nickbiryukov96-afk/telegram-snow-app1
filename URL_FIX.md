# Решение проблемы с HTTPS URL в Telegram

## Проблема
Telegram требует, чтобы URL начинался с `https://` и был доступен публично.

## Решение

### 1. Проверьте формат URL

URL должен быть в формате:
```
https://your-app-name.vercel.app
```

**НЕ используйте:**
- ❌ `http://` (без 's')
- ❌ `www.` в начале
- ❌ Пробелы или специальные символы
- ❌ Локальные адреса (`localhost`, `127.0.0.1`)

**Правильный формат:**
- ✅ `https://telegram-snow-app-xxx.vercel.app`
- ✅ `https://your-domain.com`

### 2. Проверьте доступность URL

Откройте URL в браузере:
- Должна открыться ваша страница
- В адресной строке должен быть замочек 🔒 (HTTPS)
- Не должно быть ошибок SSL

### 3. Вставка URL в BotFather

**Правильный способ:**
1. Скопируйте полный URL из Vercel (например: `https://telegram-snow-app-abc123.vercel.app`)
2. В BotFather вставьте URL **точно так, как скопировали**
3. Не добавляйте пробелы, переносы строк или другие символы

**Пример правильного URL:**
```
https://telegram-snow-app-abc123.vercel.app
```

### 4. Если URL не работает

**Проверьте:**
- URL начинается с `https://` (не `http://`)
- Нет пробелов в начале или конце
- URL полностью скопирован
- Сайт доступен в браузере

**Если все равно не работает:**
- Попробуйте скопировать URL заново из Vercel
- Убедитесь, что деплой завершен успешно
- Проверьте, что сайт открывается в браузере

### 5. Альтернатива: Использовать кастомный домен

Если у вас есть свой домен:
1. Настройте его в Vercel
2. Используйте свой домен в BotFather

## Быстрая проверка

Выполните в терминале:
```bash
# Замените YOUR_URL на ваш URL из Vercel
curl -I https://YOUR_URL
```

Должен вернуться статус `200 OK` и заголовки с `https://`.

