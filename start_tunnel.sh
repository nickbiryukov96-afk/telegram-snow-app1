#!/bin/bash

# Скрипт для запуска localtunnel и получения URL

echo "🚀 Запускаю localtunnel для порта 5173..."
echo ""
echo "⏳ Подождите несколько секунд..."
echo ""

# Запускаем localtunnel в фоне и сохраняем вывод
lt --port 5173 2>&1 | while IFS= read -r line; do
    echo "$line"
    
    # Ищем URL в выводе
    if echo "$line" | grep -q "https://.*\.loca\.lt"; then
        URL=$(echo "$line" | grep -o "https://[^ ]*\.loca\.lt")
        echo ""
        echo "✅ Туннель создан!"
        echo "📱 URL для Telegram: $URL"
        echo ""
        echo "📋 Скопируйте этот URL и используйте в BotFather:"
        echo "   1. Откройте @BotFather"
        echo "   2. Отправьте /myapps"
        echo "   3. Выберите ваше приложение"
        echo "   4. Укажите Web App URL: $URL"
        echo ""
        echo "⚠️  Туннель будет работать, пока этот скрипт запущен"
        echo "    Нажмите Ctrl+C для остановки"
    fi
done

