# Исправление: кнопка открывает браузер вместо мини-аппа

## Проблема

Кнопка "Открыть вишлист" открывает браузер вместо запуска мини-аппа внутри Telegram.

## Причины и решения

### 1. Мини-апп не настроен в BotFather (САМОЕ ВАЖНОЕ!)

**Это основная причина проблемы!** Если мини-апп не настроен в BotFather, кнопка `web_app` будет работать как обычная ссылка.

**Решение:**

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"Bot Settings"** → **"Configure Mini App"**
5. Введите URL вашего мини-аппа (тот же, что в `.env` файле)
6. Сохраните изменения

**ВАЖНО:** URL в BotFather должен **точно совпадать** с URL в `.env` файле!

### 2. URL не разрешен в настройках бота

Telegram проверяет, что URL мини-аппа разрешен для вашего бота. Если URL не совпадает с тем, что указан в BotFather, кнопка будет открывать браузер.

**Решение:**

- Убедитесь, что URL в BotFather и в `.env` **полностью идентичны**
- Проверьте, что нет лишних пробелов или символов
- URL должен начинаться с `https://`

### 3. Неправильный формат кнопки

Код бота уже использует правильный формат, но убедитесь, что он не был изменен.

**Правильный формат:**

```javascript
{
  text: '🎁 Открыть вишлист',
  web_app: {
    url: MINI_APP_URL
  }
}
```

**Неправильный формат (откроет браузер):**

```javascript
{
  text: '🎁 Открыть вишлист',
  url: MINI_APP_URL  // ❌ Это откроет браузер!
}
```

### 4. Бот не перезапущен после изменений

После изменения `.env` или кода бота нужно перезапустить бота.

**Решение:**

```bash
# Остановите бота (Ctrl+C)
# Затем запустите снова:
cd /Users/nikbiryukov/telegram-snow-app
npm run bot
```

## Пошаговая проверка

1. **Проверьте URL в `.env`:**
   ```bash
   cat .env | grep MINI_APP_URL
   ```

2. **Проверьте настройки в BotFather:**
   - Откройте @BotFather
   - `/mybots` → ваш бот → "Bot Settings" → "Configure Mini App"
   - Убедитесь, что URL совпадает с `.env`

3. **Перезапустите бота:**
   ```bash
   npm run bot
   ```

4. **Протестируйте:**
   - Отправьте боту `/start`
   - Нажмите на кнопку
   - Если все настроено правильно, мини-апп откроется внутри Telegram

## Проверка через логи бота

При запуске бота вы увидите в логах:
- `📱 Мини-апп URL: ...` - проверьте, что URL правильный
- При отправке сообщения: `📤 Отправляю сообщение с web_app кнопкой...`
- `✅ Сообщение с web_app кнопкой отправлено успешно` - если все ОК

Если видите ошибки, они укажут на проблему.

## Альтернативный способ: Menu Button

Если кнопка в сообщении не работает, можно использовать Menu Button:

1. В BotFather: "Bot Settings" → "Menu Button"
2. "Configure Menu Button"
3. Введите текст и URL
4. Кнопка появится внизу экрана при открытии бота

## Важно

- **Мини-апп ОБЯЗАТЕЛЬНО должен быть настроен в BotFather!**
- URL должен быть доступен по HTTPS
- URL в BotFather и `.env` должны совпадать
- После изменений перезапустите бота

