# Диагностика проблемы с полноэкранным режимом

## Проверка 1: Открывается ли мини-апп через web_app кнопку?

**Важно:** `expand()` работает только если мини-апп открыт через кнопку типа `web_app`, а не через обычную ссылку.

**Как проверить:**
- Если при открытии мини-аппа вы видите адресную строку браузера - это НЕ мини-апп, это обычная ссылка
- Мини-апп должен открываться внутри Telegram без адресной строки

## Проверка 2: Настроен ли мини-апп в BotFather?

1. Откройте @BotFather
2. `/mybots` → ваш бот → "Bot Settings" → "Configure Mini App"
3. Убедитесь, что URL указан и совпадает с `.env`

**Без этого `expand()` может не работать!**

## Проверка 3: Версия Telegram

`expand()` работает в:
- ✅ Telegram Desktop (последние версии)
- ✅ Telegram iOS (последние версии)
- ✅ Telegram Android (последние версии)
- ❌ Telegram Web (не поддерживает мини-аппы)

**Обновите Telegram до последней версии!**

## Проверка 4: Логи в консоли

Откройте консоль разработчика (если доступна) и проверьте логи:

**Должны быть:**
```
🔧 Инициализация Telegram WebApp...
📱 Версия: ...
📱 Платформа: ...
✅ tg.expand() вызван
```

**Если видите:**
```
⚠️ Telegram WebApp не доступен
```
Значит мини-апп открыт не через web_app кнопку.

## Проверка 5: Альтернативный способ

Попробуйте использовать Menu Button вместо кнопки в сообщении:

1. В BotFather: "Bot Settings" → "Menu Button"
2. "Configure Menu Button"
3. Введите текст и URL
4. Откройте бота и нажмите кнопку меню внизу

Menu Button обычно лучше работает с `expand()`.

## Возможные причины, почему expand() не работает

1. **Мини-апп открыт через обычную ссылку** (не web_app)
2. **Мини-апп не настроен в BotFather**
3. **Старая версия Telegram**
4. **Проблема с платформой** (некоторые версии Android/iOS могут иметь баги)
5. **Мини-апп открыт в Telegram Web** (не поддерживается)

## Решение: Принудительное разворачивание через CSS

Если `expand()` не работает, можно попробовать CSS подход (но это не идеальное решение):

```css
html, body {
  height: 100vh;
  height: 100dvh; /* Динамическая высота viewport */
  overflow: hidden;
}
```

Но лучше исправить проблему с `expand()`.

## Что проверить прямо сейчас

1. **Откройте консоль разработчика** в мини-аппе (если доступна)
2. **Проверьте логи** - что выводится?
3. **Проверьте настройки в BotFather** - мини-апп настроен?
4. **Проверьте версию Telegram** - обновлена ли?

Сообщите, что показывает консоль - это поможет понять проблему.

