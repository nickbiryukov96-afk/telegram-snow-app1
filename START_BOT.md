# Быстрый старт бота

## ✅ Что уже сделано:

1. ✅ Токен бота добавлен в `.env`
2. ✅ Зависимости установлены
3. ✅ Бот готов к запуску

## 🚀 Запуск бота:

```bash
npm run bot
```

Бот запустится и будет слушать команды `/start` и `/help`.

## 📝 Что нужно сделать:

1. **Обновить URL мини-аппа в `.env`**:
   - Откройте файл `.env`
   - Найдите строку `MINI_APP_URL=...`
   - Замените на реальный URL вашего мини-аппа из Vercel
   - Формат: `https://telegram-snow-app.vercel.app` (или ваш кастомный домен)

2. **Настроить мини-апп в BotFather**:
   - Откройте [@BotFather](https://t.me/BotFather)
   - Отправьте `/mybots`
   - Выберите вашего бота
   - Выберите "Bot Settings" → "Configure Mini App"
   - Введите URL из `.env` (тот же, что `MINI_APP_URL`)

3. **Протестировать**:
   - Найдите вашего бота в Telegram
   - Отправьте `/start`
   - Должна появиться кнопка "🎁 Открыть вишлист"
   - Нажмите на кнопку - мини-апп должен открыться

## 🔄 Запуск в фоне (для постоянной работы):

### Вариант 1: Использовать `nohup`
```bash
nohup npm run bot > bot.log 2>&1 &
```

### Вариант 2: Использовать `pm2` (рекомендуется)
```bash
npm install -g pm2
pm2 start bot-server.js --name telegram-bot
pm2 save
pm2 startup
```

## 📊 Проверка статуса бота:

Если используете `pm2`:
```bash
pm2 status
pm2 logs telegram-bot
```

## 🛑 Остановка бота:

Если запущен в терминале: нажмите `Ctrl+C`

Если запущен через `pm2`:
```bash
pm2 stop telegram-bot
pm2 delete telegram-bot
```

## ❓ Проблемы?

- **Бот не отвечает**: Проверьте, что бот запущен (`npm run bot`)
- **Кнопка не работает**: Убедитесь, что URL правильный и мини-апп настроен в BotFather
- **Ошибка токена**: Проверьте `.env` файл, токен должен быть правильным

