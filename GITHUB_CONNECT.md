# Подключение к GitHub - Пошаговая инструкция

## ✅ Что уже сделано:
- ✅ Git репозиторий инициализирован
- ✅ Все файлы добавлены в git
- ✅ Первый коммит создан

## 📝 Следующие шаги:

### 1. Создайте репозиторий на GitHub:

1. Откройте https://github.com/new
2. Заполните форму:
   - **Repository name**: `telegram-snow-app` (или любое другое имя)
   - **Description**: "Telegram Mini App для планирования зимнего отдыха"
   - **Visibility**: Public или Private (на ваше усмотрение)
   - ⚠️ **НЕ** ставьте галочки на:
     - ❌ Add a README file (у вас уже есть)
     - ❌ Add .gitignore (у вас уже есть)
     - ❌ Choose a license (можно добавить позже)
3. Нажмите **"Create repository"**

### 2. Подключите локальный репозиторий к GitHub:

После создания репозитория GitHub покажет инструкции. Выполните команды:

```bash
cd /Users/nikbiryukov/telegram-snow-app

# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/telegram-snow-app.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/telegram-snow-app.git

git branch -M main
git push -u origin main
```

### 3. Проверьте подключение:

```bash
git remote -v
```

Должно показать:
```
origin  https://github.com/YOUR_USERNAME/telegram-snow-app.git (fetch)
origin  https://github.com/YOUR_USERNAME/telegram-snow-app.git (push)
```

## 🚀 После подключения к GitHub:

1. **Задеплойте на Vercel:**
   - Перейдите на https://vercel.com
   - Войдите через GitHub
   - Нажмите "Add New Project"
   - Выберите репозиторий `telegram-snow-app`
   - Нажмите "Deploy"

2. **Настройте Web App в BotFather:**
   - Откройте [@BotFather](https://t.me/BotFather)
   - Отправьте `/newapp`
   - Укажите URL из Vercel

## 💡 Полезные команды:

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log --oneline

# Добавить изменения и закоммитить
git add .
git commit -m "Описание изменений"
git push

# Посмотреть подключенные remote репозитории
git remote -v
```

