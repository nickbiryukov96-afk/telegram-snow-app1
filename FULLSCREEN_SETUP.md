# Как запустить мини-апп на полный экран из чата

## ✅ Что уже настроено в коде

В файле `src/App.tsx` уже есть настройка для полноэкранного режима:
- `tg.expand()` - разворачивает мини-апп на полный экран
- `tg.setBackgroundColor()` - устанавливает цвет фона
- `tg.setHeaderColor()` - устанавливает цвет заголовка

## 📋 Пошаговая инструкция

### Шаг 1: Настройка мини-аппа в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"Bot Settings"** → **"Configure Mini App"**
5. Введите URL вашего мини-аппа:
   - Для продакшена: `https://ваш-проект.vercel.app`
   - Для тестирования: ваш Cloudflare Tunnel URL
6. Сохраните изменения

### Шаг 2: Настройка кнопки меню (Menu Button)

1. В @BotFather выберите вашего бота
2. Выберите **"Bot Settings"** → **"Menu Button"**
3. Выберите **"Configure Menu Button"**
4. Введите текст кнопки (например: "Открыть вишлист")
5. Введите URL вашего мини-аппа (тот же, что в шаге 1)
6. Сохраните

После этого кнопка появится внизу экрана при открытии бота.

### Шаг 3: Использование кнопки web_app в сообщениях

Если у вас есть бот-сервер (`bot-server.js`), он уже настроен на отправку кнопки с типом `web_app`. 

Когда пользователь нажимает на кнопку с типом `web_app`, мини-апп автоматически открывается в полноэкранном режиме благодаря `tg.expand()` в коде.

### Шаг 4: Проверка работы

1. Найдите вашего бота в Telegram
2. Откройте бота (или напишите `/start`)
3. Нажмите на кнопку меню внизу экрана или на кнопку в сообщении
4. Мини-апп должен открыться **на полный экран**

## 🔍 Как проверить, что мини-апп открывается на полный экран

После открытия мини-аппа:
- ✅ Экран должен занимать всю область (без видимых границ чата)
- ✅ Внизу должна быть кнопка "Back" для возврата в чат
- ✅ Фон вокруг мини-аппа должен быть темным (черным)

## ⚙️ Дополнительные настройки в коде

Если нужно изменить поведение полноэкранного режима, откройте `src/App.tsx`:

```typescript
useEffect(() => {
  if (typeof window !== 'undefined' && window.Telegram?.WebApp) {
    const tg = window.Telegram.WebApp;
    
    tg.ready();        // Готовность мини-аппа
    tg.expand();       // Разворачивание на полный экран
    
    tg.setBackgroundColor('#000000');  // Цвет фона
    tg.setHeaderColor('#000000');      // Цвет заголовка
  }
}, []);
```

## 🐛 Решение проблем

### Мини-апп не открывается на полный экран

1. **Проверьте, что мини-апп настроен в BotFather:**
   - Откройте @BotFather → /mybots → ваш бот → Bot Settings → Configure Mini App
   - Убедитесь, что URL правильный

2. **Проверьте, что используется кнопка типа `web_app`:**
   - Обычная URL-кнопка откроет сайт в браузере
   - Только кнопка `web_app` открывает мини-апп в Telegram

3. **Проверьте консоль браузера:**
   - Откройте DevTools (если доступно)
   - Убедитесь, что нет ошибок JavaScript

4. **Проверьте версию Telegram:**
   - Мини-аппы работают в Telegram Desktop, iOS, Android
   - Убедитесь, что у вас актуальная версия

### Мини-апп открывается, но не на полный экран

1. Убедитесь, что в коде есть `tg.expand()`
2. Проверьте, что `tg.ready()` вызывается перед `tg.expand()`
3. Убедитесь, что мини-апп открыт через кнопку `web_app`, а не через обычную ссылку

## 📱 Открытие из группового чата

Для открытия мини-аппа из группового чата:

1. Добавьте бота в группу
2. Напишите боту `/start` в группе
3. Бот отправит сообщение с кнопкой `web_app`
4. Нажмите на кнопку - мини-апп откроется на полный экран

## 🔗 Полезные ссылки

- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [Telegram Bot API - Web App](https://core.telegram.org/bots/api#inlinekeyboardbutton)
- [BotFather](https://t.me/BotFather)

