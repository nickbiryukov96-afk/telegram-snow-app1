# Настройка проекта для деплоя через GitHub

## ✅ Что уже есть в проекте:

1. **package.json** - со скриптом `build` ✅
2. **vite.config.ts** - конфигурация сборщика ✅
3. **vercel.json** - конфигурация для Vercel ✅
4. **Исходный код** - папки `src/` и `public/` ✅
5. **.gitignore** - правильно настроен (исключает `dist/` и `node_modules/`) ✅

## 📝 Что нужно сделать:

### 1. Инициализировать Git (если еще не сделано):

```bash
cd /Users/nikbiryukov/telegram-snow-app
git init
git add .
git commit -m "Initial commit"
```

### 2. Создать репозиторий на GitHub:

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий (например: `telegram-snow-app`)
3. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)

### 3. Подключить локальный репозиторий к GitHub:

```bash
git remote add origin https://github.com/YOUR_USERNAME/telegram-snow-app.git
git branch -M main
git push -u origin main
```

### 4. Задеплоить на Vercel:

**Вариант A: Через веб-интерфейс**
1. Перейдите на https://vercel.com
2. Войдите через GitHub
3. Нажмите "Add New Project"
4. Выберите ваш репозиторий `telegram-snow-app`
5. Vercel автоматически определит настройки:
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
6. Нажмите "Deploy"
7. Получите URL (например: `https://telegram-snow-app.vercel.app`)

**Вариант B: Через Vercel CLI**
```bash
npm install -g vercel
vercel login
vercel --prod
```

### 5. Настроить Web App в BotFather:

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newapp` или `/myapps`
3. Выберите вашего бота
4. Укажите URL из Vercel (например: `https://telegram-snow-app.vercel.app`)

## ⚠️ Важно:

- ✅ **НЕ коммитьте** папку `dist/` в Git (она уже в .gitignore)
- ✅ **НЕ коммитьте** `node_modules/` (уже в .gitignore)
- ✅ Vercel автоматически соберет проект при каждом push в GitHub
- ✅ После деплоя каждое обновление в GitHub будет автоматически деплоиться

## 🔄 Обновление проекта:

После изменений в коде:

```bash
git add .
git commit -m "Update app"
git push
```

Vercel автоматически пересоберет и задеплоит новую версию!

## 📦 Что должно быть в GitHub:

```
telegram-snow-app/
├── src/              ✅ Исходный код
├── public/           ✅ Статические файлы (изображения)
├── package.json      ✅ Зависимости и скрипты
├── vite.config.ts    ✅ Конфигурация Vite
├── vercel.json       ✅ Конфигурация Vercel
├── index.html        ✅ Главный HTML файл
├── tsconfig.json     ✅ Конфигурация TypeScript
└── .gitignore        ✅ Игнорируемые файлы
```

## ❌ Что НЕ должно быть в GitHub:

- `dist/` - собирается при деплое
- `node_modules/` - устанавливается при деплое
- `.env` - секретные данные (если есть)

