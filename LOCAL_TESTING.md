# Локальное тестирование Mini App

## ✅ Что настроено:

1. **Dev сервер запущен** на `http://localhost:5173`
2. **Эмуляция Telegram WebApp** включена в `index.html`
3. Все функции Telegram API эмулируются локально

## 🚀 Как тестировать:

### Вариант 1: В браузере (быстро)

1. **Откройте браузер:**
   ```
   http://localhost:5173
   ```

2. **Что проверить:**
   - ✅ Приложение открывается
   - ✅ Нижняя навигация работает
   - ✅ Все экраны переключаются
   - ✅ Вишлист работает
   - ✅ Изображения загружаются
   - ✅ В консоли браузера видны логи Telegram API

3. **Откройте консоль браузера (F12):**
   - Должны быть логи: "Telegram WebApp ready", "Telegram WebApp expanded"
   - Проверьте, нет ли ошибок

### Вариант 2: В Telegram через локальный туннель

1. **Установите localtunnel (если еще не установлен):**
   ```bash
   npm install -g localtunnel
   ```

2. **Создайте туннель:**
   ```bash
   lt --port 5173
   ```

3. **Скопируйте HTTPS URL** (например: `https://abc123.loca.lt`)

4. **Настройте в BotFather:**
   - Откройте @BotFather
   - `/myapps` → выберите приложение
   - Укажите URL из localtunnel

5. **Протестируйте в Telegram**

### Вариант 3: Через ngrok

1. **Установите ngrok:**
   ```bash
   brew install ngrok
   ```

2. **Создайте туннель:**
   ```bash
   ngrok http 5173
   ```

3. **Используйте HTTPS URL** из ngrok в BotFather

## 🔍 Что проверять:

- [ ] Приложение открывается без ошибок
- [ ] Навигация работает плавно
- [ ] Все три экрана отображаются
- [ ] Вишлист: можно выбрать подарок
- [ ] Изображения загружаются
- [ ] Нет ошибок в консоли
- [ ] Telegram API эмулируется (логи в консоли)

## 🐛 Отладка:

### Проблема: Страница не открывается
- Проверьте, что dev сервер запущен: `npm run dev`
- Проверьте URL: `http://localhost:5173`

### Проблема: Telegram API не работает
- Откройте консоль браузера (F12)
- Проверьте, что `telegram-mock.js` загружается
- Должны быть логи: "Telegram WebApp ready"

### Проблема: Изображения не загружаются
- Проверьте, что файлы в папке `public/`
- Проверьте пути к изображениям в коде

## 📝 Полезные команды:

```bash
# Запустить dev сервер
npm run dev

# Остановить dev сервер
# Нажмите Ctrl+C в терминале

# Проверить, что сервер работает
curl http://localhost:5173
```

## ⚠️ Важно:

- Эмуляция Telegram API работает только в браузере
- Для полного тестирования нужен реальный Telegram
- Используйте туннель (localtunnel/ngrok) для тестирования в Telegram

