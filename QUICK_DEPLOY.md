# Быстрый деплой для тестирования

## Проблема с localtunnel
Localtunnel требует пароль, что неудобно для тестирования.

## Решение: Задеплойте на Vercel (рекомендуется)

### Вариант 1: Через веб-интерфейс (самый быстрый)

1. **Откройте Vercel:**
   - https://vercel.com/dashboard
   - Войдите через GitHub

2. **Импортируйте проект:**
   - Нажмите "Add New Project"
   - Выберите репозиторий: `nickbiryukov96-afk/telegram-snow-app`
   - Нажмите "Import"

3. **Настройки (Vercel определит автоматически):**
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Нажмите "Deploy"

4. **Получите URL:**
   - После деплоя (1-2 минуты) получите Production URL
   - Формат: `https://telegram-snow-app.vercel.app`

5. **Используйте в BotFather:**
   - Откройте @BotFather
   - `/myapps` → выберите приложение
   - Укажите URL из Vercel

### Вариант 2: Через Vercel CLI

```bash
cd /Users/nikbiryukov/telegram-snow-app
npm install -g vercel
vercel login
vercel --prod
```

## Альтернатива: Cloudflare Tunnel (без пароля)

Если хотите использовать туннель без пароля:

1. **Установите cloudflared:**
   ```bash
   brew install cloudflared
   ```

2. **Запустите туннель:**
   ```bash
   cloudflared tunnel --url http://localhost:5173
   ```

3. **Скопируйте HTTPS URL** и используйте в BotFather

## Почему Vercel лучше для тестирования:

- ✅ Быстро (деплой за 1-2 минуты)
- ✅ Надежно (стабильный URL)
- ✅ Без паролей
- ✅ HTTPS автоматически
- ✅ Можно использовать для продакшена

## После деплоя на Vercel:

1. Скопируйте Production URL
2. Настройте в BotFather
3. Протестируйте в Telegram
4. Готово! 🎉

