# Настройка Telegram бота для команды /start

## Шаг 1: Получение токена бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot` (если бота еще нет)
3. Следуйте инструкциям для создания бота
4. Сохраните токен бота (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Настройка переменных окружения

1. Скопируйте файл `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте файл `.env` и заполните:
   ```env
   BOT_TOKEN=ваш_токен_бота_здесь
   MINI_APP_URL=https://ваш-сайт.vercel.app
   ```

## Шаг 3: Установка зависимостей

```bash
npm install
```

Это установит:
- `node-telegram-bot-api` - библиотека для работы с Telegram Bot API
- `dotenv` - для загрузки переменных окружения

## Шаг 4: Запуск бота

### Локальный запуск:
```bash
npm run bot
```

### Запуск с автоперезагрузкой (для разработки):
```bash
npm run bot:dev
```

После запуска вы увидите:
```
✅ Бот запущен и готов к работе!
🤖 Бот слушает команды...
📱 Мини-апп URL: https://...
```

## Шаг 5: Проверка работы

1. Найдите вашего бота в Telegram (по имени, которое вы указали при создании)
2. Отправьте команду `/start`
3. Бот должен ответить сообщением с кнопкой "🎁 Открыть вишлист"
4. Нажмите на кнопку - мини-апп должен открыться

## Шаг 6: Настройка мини-аппа в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Configure Mini App"
5. Введите URL вашего мини-аппа (тот же, что в `.env`)

## Деплой бота на сервер

Для работы бота 24/7 его нужно запустить на сервере. Варианты:

### Вариант 1: Vercel (только для веб-приложения)
Vercel не подходит для long-polling ботов. Используйте другие варианты.

### Вариант 2: Railway
1. Зарегистрируйтесь на [Railway](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Добавьте переменные окружения `BOT_TOKEN` и `MINI_APP_URL`
5. Railway автоматически запустит бота

### Вариант 3: Heroku
1. Зарегистрируйтесь на [Heroku](https://heroku.com)
2. Создайте новое приложение
3. Подключите GitHub репозиторий
4. Добавьте переменные окружения в настройках
5. Запустите приложение

### Вариант 4: VPS (например, DigitalOcean, AWS)
1. Арендуйте VPS
2. Установите Node.js
3. Клонируйте репозиторий
4. Установите зависимости: `npm install`
5. Настройте `.env` файл
6. Запустите бота через `pm2` или `systemd`:
   ```bash
   npm install -g pm2
   pm2 start bot-server.js --name telegram-bot
   pm2 save
   pm2 startup
   ```

## Структура файлов

- `bot-server.js` - основной файл сервера бота
- `.env` - переменные окружения (не коммитьте в git!)
- `.env.example` - пример файла с переменными окружения

## Команды бота

- `/start` - Открыть вишлист (показывает кнопку с мини-аппом)
- `/help` - Показать справку

## Добавление новых команд

Чтобы добавить новую команду, откройте `bot-server.js` и добавьте обработчик:

```javascript
bot.onText(/\/новаякоманда/, (msg) => {
  const chatId = msg.chat.id;
  bot.sendMessage(chatId, 'Ответ на новую команду');
});
```

## Устранение проблем

### Бот не отвечает на команды
- Проверьте, что токен бота правильный в `.env`
- Убедитесь, что бот запущен (`npm run bot`)
- Проверьте логи на наличие ошибок

### Кнопка не открывает мини-апп
- Убедитесь, что URL мини-аппа правильный и доступен по HTTPS
- Проверьте, что мини-апп настроен в BotFather
- Убедитесь, что домен разрешен в настройках бота

### Ошибка "BOT_TOKEN не найден"
- Создайте файл `.env` в корне проекта
- Добавьте строку `BOT_TOKEN=ваш_токен`
- Перезапустите бота

