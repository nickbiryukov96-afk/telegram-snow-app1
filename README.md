# Telegram Mini App - Зимние Горы

Полноэкранное Telegram Mini App с темой зимних гор, сноуборда и Грузии (вино и хинкали).

## Технологии

- **React 19** + **TypeScript**
- **Vite** - сборщик проекта
- **styled-components** - стилизация компонентов
- **Telegram WebApp API** - интеграция с Telegram

## Структура проекта

```
src/
├── components/
│   └── BottomNav.tsx      # Нижняя навигация
├── screens/
│   ├── Home.tsx          # Главный экран
│   ├── LeftTab.tsx       # Левая вкладка (Склоны)
│   └── RightTab.tsx      # Правая вкладка (Грузия)
├── types/
│   └── telegram.d.ts     # Типы для Telegram WebApp API
├── App.tsx               # Главный компонент с роутингом
├── main.tsx              # Точка входа
├── theme.ts              # Цветовая палитра и тема
└── index.css             # Глобальные стили
```

## Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Просмотр собранного проекта
npm run preview
```

## Особенности

### Дизайн
- **Цветовая палитра**: холодные оттенки (голубой, белый, бирюзовый, тёмно-синий)
- **Градиенты**: горные пейзажи, снежные склоны, ледяные текстуры
- **Анимации**: плавные переходы между экранами, эффекты наведения
- **Адаптивность**: оптимизировано для мобильных устройств

### Функционал
- Полноэкранный режим в Telegram
- Три экрана с переключением через нижнюю навигацию
- Анимации при смене табов
- Фиксированная нижняя навигация

## Тестирование

### Быстрое локальное тестирование

```bash
# Запустите dev сервер
npm run dev
```

Откройте браузер по адресу `http://localhost:5173`

**Для эмуляции Telegram WebApp API:**
1. Раскомментируйте строку в `index.html`: `<script src="/telegram-mock.js"></script>`
2. Перезапустите dev сервер

### Тестирование в Telegram

**Вариант 1: Через локальный туннель (ngrok)**
```bash
# Установите ngrok
brew install ngrok  # macOS
# или скачайте с https://ngrok.com

# Запустите dev сервер
npm run dev

# В другом терминале создайте туннель
ngrok http 5173
```

Используйте HTTPS URL из ngrok для настройки Web App в [@BotFather](https://t.me/BotFather)

**Вариант 2: Загрузка на хостинг**
1. Соберите проект: `npm run build`
2. Загрузите файлы из `dist/` на хостинг (Vercel, Netlify, GitHub Pages)
3. Настройте Web App URL в [@BotFather](https://t.me/BotFather)

📖 **Подробная инструкция:** см. [TESTING.md](./TESTING.md)

## Использование в Telegram

1. Соберите проект: `npm run build`
2. Загрузите файлы из папки `dist/` на ваш хостинг
3. Создайте бота через [@BotFather](https://t.me/BotFather)
4. Настройте Web App URL в настройках бота
5. Откройте бота в Telegram и запустите Mini App

## Настройка Telegram WebApp

Приложение автоматически настраивает:
- Полноэкранный режим (`expand()`)
- Цвет заголовка и фона
- Готовность приложения (`ready()`)

Все настройки находятся в `src/App.tsx` в хуке `useEffect`.

## Разработка

### Добавление нового экрана

1. Создайте файл в `src/screens/`
2. Добавьте новый таб в `BottomNav.tsx`
3. Обновите тип `Tab` и состояние в `App.tsx`

### Изменение темы

Все цвета и стили находятся в `src/theme.ts`. Измените значения для обновления дизайна.

## Лицензия

MIT
