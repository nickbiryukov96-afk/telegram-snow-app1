# Инструкция по запуску туннеля для тестирования

## 🚀 Быстрый способ

### Вариант 1: Использовать скрипт (рекомендуется)

```bash
cd /Users/nikbiryukov/telegram-snow-app
./start_tunnel.sh
```

Скрипт автоматически:
- Запустит localtunnel
- Покажет URL для Telegram
- Даст инструкции по настройке

### Вариант 2: Вручную

1. **Убедитесь, что dev сервер запущен:**
   ```bash
   cd /Users/nikbiryukov/telegram-snow-app
   npm run dev
   ```

2. **В другом терминале запустите туннель:**
   ```bash
   lt --port 5173
   ```

3. **Скопируйте URL** который покажет localtunnel (например: `https://abc123.loca.lt`)

4. **Настройте в BotFather:**
   - Откройте @BotFather
   - Отправьте `/myapps`
   - Выберите ваше приложение
   - Укажите Web App URL: вставьте URL из localtunnel

## ⚠️ Важно:

- Dev сервер должен быть запущен на порту 5173
- Туннель работает только пока запущен процесс
- URL меняется при каждом перезапуске туннеля
- Используйте HTTPS URL (localtunnel автоматически дает HTTPS)

## 🔍 Проверка:

После настройки URL в BotFather:
1. Откройте вашего бота в Telegram
2. Нажмите на кнопку с Web App
3. Приложение должно открыться

## 🛑 Остановка:

Нажмите `Ctrl+C` в терминале где запущен туннель

