# Как открыть мини-апп из группового чата

## Шаг 1: Настройка бота через BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot` (если бота еще нет)
3. Следуйте инструкциям для создания бота
4. Сохраните токен бота

## Шаг 2: Добавление мини-аппа к боту

1. В @BotFather отправьте команду `/mybots`
2. Выберите вашего бота
3. Выберите "Bot Settings" → "Configure Mini App"
4. Введите URL вашего мини-аппа (например, `https://telegram-snow-app-xxx.vercel.app`)
5. Сохраните изменения

## Шаг 3: Добавление бота в групповой чат

1. Добавьте вашего бота в групповой чат
2. Сделайте бота администратором (опционально, но рекомендуется)
3. Убедитесь, что у бота есть права на отправку сообщений

## Шаг 4: Создание кнопки для открытия мини-аппа

Есть несколько способов добавить кнопку для открытия мини-аппа:

### Способ 1: Через команду бота

Создайте простой бот, который будет отправлять сообщение с кнопкой. Пример на Python:

```python
from telegram import Bot, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import Updater, CommandHandler

TOKEN = 'ВАШ_ТОКЕН_БОТА'
APP_URL = 'https://ваш-сайт.vercel.app'  # URL вашего мини-аппа

def start(update, context):
    keyboard = [
        [InlineKeyboardButton("🎁 Открыть вишлист", web_app=APP_URL)]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    update.message.reply_text(
        'Нажмите кнопку ниже, чтобы открыть вишлист:',
        reply_markup=reply_markup
    )

def main():
    updater = Updater(TOKEN, use_context=True)
    dp = updater.dispatcher
    dp.add_handler(CommandHandler("start", start))
    updater.start_polling()
    updater.idle()

if __name__ == '__main__':
    main()
```

### Способ 2: Через BotFather (меню бота)

1. В @BotFather выберите вашего бота
2. Выберите "Bot Settings" → "Menu Button"
3. Выберите "Configure Menu Button"
4. Введите текст кнопки (например, "Открыть вишлист")
5. Введите URL вашего мини-аппа

После этого кнопка появится внизу экрана при открытии бота.

### Способ 3: Через команду /start в группе

Когда пользователь напишет `/start` боту в группе, бот может отправить сообщение с кнопкой для открытия мини-аппа.

## Шаг 5: Проверка работы

1. Откройте групповой чат
2. Напишите боту `/start` или нажмите на кнопку меню
3. Нажмите на кнопку "Открыть вишлист" или аналогичную
4. Мини-апп должен открыться

## Важные замечания

1. **HTTPS обязателен**: Мини-апп должен быть доступен по HTTPS
2. **Домен должен быть разрешен**: Убедитесь, что домен вашего мини-аппа добавлен в настройках бота
3. **Права бота**: В групповом чате бот должен иметь права на отправку сообщений
4. **Кнопка web_app**: Используйте тип кнопки `web_app` для открытия мини-аппа

## Альтернативный способ: Deep Link

Вы также можете создать deep link для открытия мини-аппа напрямую:

```
https://t.me/ваш_бот?startapp=app_name
```

Или использовать кнопку в сообщении с параметром `web_app`.

## Полезные ссылки

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [BotFather](https://t.me/BotFather)

